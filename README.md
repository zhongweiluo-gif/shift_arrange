# 📅 shift_arrange (スマートシフト自動作成システム)

`shift_arrange` は、Google Drive API と数理最適化ソルバー（PuLP / HiGHS）を活用し、複雑な勤務条件や制約を考慮したシフト表を全自動で計算・生成する Python アプリケーションです。

---

## ✨ 主な機能 (Key Features)

* **📥 クラウドデータ連携**
  Google Drive API を利用し、クラウド上の Google スプレッドシート（前月および当月データ）をそのままの書式（セルカラーやレイアウト）を保持したまま自動取得・分割します。
* **🧠 高度な数理最適化 (PuLP + HiGHS)**
  単純なランダム生成ではなく、数理最適化モデルを用いて以下の複雑な制約を満たす最適なシフトを算出します：
  * **OJT 師弟関係の遵守**: 実習生が夜勤（N）に入る際、必ず指定された専任メンターを同一シフトに配置。
  * **勤務間隔・連続出勤の制御**: 夜勤後の明け（明）および休日の確保、6連勤以上の厳格な防止。
  * **公休・祝日管理**: 当月の赤日（土日・祝日）を自動判定し、優先的な休暇割り当てを実施。
  * **連休の評価**: 2連休〜4連休以上のまとまった休暇に対して報酬スコアを付与し、働きやすいシフトを生成。
* **🎨 役割後処理 & Excel 自動スタイリング**
  `openpyxl` を使用し、最適化されたシフト結果を Excel ファイルへ自動書き込み。勤務種別（D/D2/D3/N/明/休/在メ など）に応じてセルカラーやフォントを自動で装飾します。
* **📦 GitHub Actions によるクロスプラットフォームビルド**
  PyInstaller を使用し、Windows（`.exe`）および macOS 用の実行可能ファイルを GitHub Actions 上で自動ビルドします（Python 環境がない PC でも実行可能）。

---

## 🛠 技術スタック (Tech Stack)

| カテゴリ | 使用技術 / ライブラリ |
| :--- | :--- |
| **言語** | Python 3.10 |
| **数理最適化** | PuLP, HiGHS / CBC Solver |
| **API / クラウド** | Google Drive API v3, Google OAuth 2.0 |
| **データ操作** | openpyxl |
| **CI/CD / ビルド** | GitHub Actions, PyInstaller |

---

## 🚀 実行手順 (Workflow)

プログラムを起動すると、対話型のコンソールメニューが表示されます。状況に応じてステップを選択できます：

```text
==================================================
🌟 スマートシフト作成システム - メインメニュー 🌟
==================================================
  [1] クラウドから最新データを取得
  [2] PuLP最適化計算を実行 (シフト生成)
  [3] 役割とルールの後処理 (最終調整)
  --------------------------------------------------
  [4] 全自動一括実行 (1~3を連続実行)
  [0] システムを終了
==================================================